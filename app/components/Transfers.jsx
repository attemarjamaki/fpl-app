import React from "react";

const Transfers = ({ managerData }) => {
  console.log(managerData.latestTransfers);

  return (
    <div>
      Transfers
      <div>test</div>
    </div>
  );
};

export default Transfers;
